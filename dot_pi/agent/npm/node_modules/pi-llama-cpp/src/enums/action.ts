/** The possible actions for the /models command */
export enum Action {
  SWITCH = "Switch model",
  RETRY = "Retry",
  LOAD = "Load & switch",
  UNLOAD = "Unload",
  INFO = "Info",
  CANCEL = "Cancel",
}
